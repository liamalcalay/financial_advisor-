# Keep this file safe to commit. Put the real values in .env instead.
AI_PROVIDER=gemini
GEMINI_MODEL=gemini-3.6-flash
GEMINI_API_KEY=replace_with_your_key

# Use "demo" for a public Streamlit deployment with sample holdings and no AI calls.
APP_MODE=local
